import React from "react";
import styles from "../../css/audio/AudioFile.module.scss";
import { PropTypes } from "prop-types";

const AudioFile = () => {
	return (
		<div className={styles.AudioFile}>
			{/*  */}
			{/*  */}
			{/*  */}
		</div>
	);
};

export default AudioFile;

AudioFile.defaultProps = {};

AudioFile.propTypes = {};
